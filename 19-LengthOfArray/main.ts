//creating array of guests
let guestList: string[] = ["Ali","Mohsin", "Hayat","Fareed","Mustaqeem"];

console.log(guestList);
//using length comand
let guestCount: number = guestList.length;

//print msg indicating the number of guests you are inviting to dinner
console.log(`I am inviting only ${guestCount} guests.`);
