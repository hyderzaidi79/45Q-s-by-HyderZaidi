function make_shirt(size:string, printmsg:string){
console.log(`You selected ${size} size shirt with ${printmsg} prints on it.`)
}
make_shirt ("Large", `"Code with Hamza"`)

make_shirt("Small",`"Hamza"`);